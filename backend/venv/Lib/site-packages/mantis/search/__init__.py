"""도구 검색 — graph-tool-call 기반 시맨틱 검색."""

try:
    from mantis.search.graph_search import GraphToolManager, GraphToolConfig

    __all__ = ["GraphToolManager", "GraphToolConfig"]

    # 0.15+ 선택 기능
    try:
        from graph_tool_call.compressor import CompressConfig
        from graph_tool_call.workflow import WorkflowPlan
        __all__ += ["CompressConfig", "WorkflowPlan"]
    except ImportError:
        pass

except ImportError:
    # graph-tool-call 미설치 시 빈 export
    __all__ = []
